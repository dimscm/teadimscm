workflows:
  android-debug:
    name: MoneyPrinter Mobile - debug APK
    max_build_duration: 60
    # The free plan's build minutes are on the macOS instances; linux_x2 and
    # windows_x2 need a paid plan. macOS images carry the Android SDK too, so an
    # Android build runs there unchanged.
    instance_type: mac_mini_m2

    environment:
      java: 17

    scripts:
      - name: Point Gradle at the Android SDK on the build machine
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/local.properties"

      - name: Build the debug APK
        script: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --stacktrace

    artifacts:
      - app/build/outputs/**/*.apk

    # To get the APK emailed to you as well, add your address here from the
    # Codemagic UI rather than committing it to a public repository:
    #
    # publishing:
    #   email:
    #     recipients:
    #       - you@example.com
